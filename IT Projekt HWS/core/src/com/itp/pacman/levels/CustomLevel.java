package com.itp.pacman.levels;

import com.itp.pacman.PacMan;

public class CustomLevel extends GameLevel{
	public CustomLevel(PacMan game, String regionName, int fieldSizeX, int fieldSizeY, int tileSizeX, int tileSizeY) {
		super(game, regionName, fieldSizeX, fieldSizeY, tileSizeX, tileSizeY);
	}
	
	//TODO: Create a a hashmap of cutsom levels?
}